services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: doit
      POSTGRES_PASSWORD: doit
      POSTGRES_DB: doit_agent
    ports:
      - "5432:5432"
    volumes:
      - doit_pgdata:/var/lib/postgresql/data
volumes:
  doit_pgdata:
