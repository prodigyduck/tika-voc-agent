# tika-agent E2E 체크리스트 (MVP 게이트)

실행 조건: postgres 기동 + `.env`의 LLM_API_KEY 설정 + 백엔드/프론트 기동.

## 자동 (qa-engineer)

- [x] `pytest -q` 전체 통과
- [x] `python scripts/lint_manual.py` 통과
- [x] `cd frontend && npm test && npm run build` 통과
- [x] `python scripts/e2e_check.py` 6/6 통과

## 수동 — MVP 완료 기준 (스펙 §9)

1. [x] 챗에서 "완료한 할 일이 목록에서 안 보여요" 입력 → 사용법문의 배지 + 출처 포함 답변 표시
2. [x] 챗에서 "앱을 켜면 바로 꺼집니다" 입력 → ⚠ 사람 확인 필요 표시 + 접수 안내 문구
3. [x] 챗에서 메뉴얼에 없는 요청(예: 테마 색 변경) → 지어내지 않고 "찾지 못했습니다" 안내 + 에스컬레이션
4. [x] 대시보드(/dashboard)에서 총 VOC·유형별 분포·미해결 에스컬레이션 수 표시
5. [x] 에스컬레이션 테이블에서 "해결 완료" 클릭 → open→resolved 전환 및 다시 열기 동작
6. [x] VOC 이력 필터(유형/에스컬레이션만) 동작

## 결과 기록

| 일자 | 버전(커밋) | 자동 게이트 | 수동 6항목 | 판정 |
|------|-----------|-------------|-----------|------|
| 2026-08-19 | 0ea6469 | 52/52·린트·6/6·빌드·E2E 5/5 통과 | 6/6 통과 (브라우저 실동작 확인) | MVP 게이트 통과 |
| 2026-08-19 | 86e719f | 54/54·린트·E2E 6/6 통과 | — (검색 품질 재검증) | 비고 2건 해소 확인 |
| 2026-08-20 | 8406f25 | 54/54·린트·6/6·빌드·E2E 6/6 통과 | — (doit 전환 검증) | 서비스 앱 doit 전환 완료 |
| 2026-08-22 | 686d9ef | 80/80·린트·vitest 11/11·빌드·라이브 E2E 6/6(채점 포함)·백필 0건(종료 0) | 대시보드 확인 (Playwright): 채점 배지 15/15x4·미채점 —·평균 점수/재료부족 큐 카드·"재료부족" 필터 동작·채팅→비동기 채점→배지 전환 | Phase 2 LLM-as-a-Judge 게이트 통과 |

비고 (2026-08-19 실행 환경·관찰):
- 실행 환경: LLM z.ai glm-4.7(OpenAI 호환 코딩 엔드포인트), DB는 sqlite(`.env`의 `DATABASE_URL`) — postgres 조건은 로컬 실행에서 sqlite로 대체
- 수동 1: "...어디서 확인하나요?"가 붙은 질문형은 사용법문의로 분류·출처 표시 확인. 끝이 짧은 진술형("완료한 할 일이 목록에서 안 보여요")은 버그제보로 분류됨 — 어느 쪽도 합리적 판단이나, 진술형 VOC의 사용법 안내 누락 가능성은 분류 프롬프트 개선 과제
- 수동 3: 기능요청 경로는 설계상 접수 안내로 응답(지어내지 않음 + 에스컬레이션 확인). "찾지 못했습니다" 문구는 사용법문의에서 청크가 없을 때의 경로
- E2E 시나리오 2("할 일을 어떻게 삭제하나요?")는 분류는 통과했으나 답변이 "메뉴얼에 없음"으로 나옴 — 어형 차이("삭제하나요" vs "삭제하기")로 검색이 매칭 못 한 것(토크나이저 어형 한계, 알려진 Minor)

2026-08-19 재검증 (86e719f): 위 비고의 두 과제 해소 — ①분류 프롬프트에 진술형 구분 규칙·예시 추가로 진술형 VOC("완료한 할 일이 목록에서 안 보여요")가 사용법문의로 분류되고 출처 있는 안내 답변 생성 ②검색에 어형 정규화 제목 매칭 추가로 "삭제하나요" 질문이 `02-managing-todos#할 일 삭제하기`를 첫 출처로 한 실제 삭제 절차 답변. E2E 시나리오 6개로 확장해 회귀 방지.

2026-08-22 Phase 2 (686d9ef) 비고·관찰:
- 수동 확인은 Playwright(백엔드 8001 + 프론트 프리뷰 4173)로 실동작 검증 — 메뉴얼에 없는 "CSV 내보내기" 사용법문의가 정직한 부정 안내("메뉴얼에 없음")로 응답, 비동기 채점 15/15·원인 "해당없음" 판정 후 대시보드 배지가 "—"→"15/15"로 전환되는 것까지 확인
- 채점 관찰: glm-4.7 채점자는 "출처에 없다는 사실을 정확히 안내"를 만점으로 판정. 설계 의도(완결성 감점→재료부족 큐)와 다르게, 출처가 붙은 정직한 부정 답변은 재료부족 큐에 잡히지 않음. 결정론적 큐(사용법문의+에스컬레이션+무출처)는 별도 작동. Phase 3 착수 전 채점 프롬프트에 "해결책이 출처에 없으면 완결성 감점" 규칙 추가 검토 권고
- 게이트 중 발견·수정: 파일 sqlite 엔진이 StaticPool 단일 커널션 공유로 동시 요청 시 SIGSEGV 크래시(대시보드 stats+vocs 병렬 fetch로 재현) → 커넥션 풀 분리 픽스(회귀 테스트 포함, 동시 4요청×8라운드 생존 확인)
